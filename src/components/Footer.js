import React from 'react'
import styled from 'styled-components'

function Footer() {
    return (
        <Container>
          This is for the FOOTER......(under development)
        </Container>
    )
}

export default Footer

const Container=styled.div`
  height: 180px;
  background-color: grey;
  font-size: 25px;
  display: flex;
  justify-content: center;
  align-items: center;
`
